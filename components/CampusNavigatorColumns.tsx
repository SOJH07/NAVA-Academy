import React from 'react';

// This component has been replaced by CampusNavigatorFloorPlan.tsx
// It is kept to avoid breaking imports in other files, but its content is removed.
const CampusNavigatorColumns: React.FC = () => {
    return null;
};

export default CampusNavigatorColumns;
